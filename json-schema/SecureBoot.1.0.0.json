{
	"$schema": "http://redfish.dmtf.org/schemas/v1/redfish-schema.1.0.0.json",
	"title": "#SecureBoot.1.0.0.SecureBoot",
	"$ref": "#/definitions/SecureBoot",
	"definitions": {
		"SecureBoot": {
			"type": "object",
			"patternProperties": {
				"^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message|Privileges)\\.[a-zA-Z_][a-zA-Z0-9_.]+$": {
					"type": [
						"array",
						"boolean",
						"number",
						"null",
						"object",
						"string"
					],
					"description": "This property shall specify a valid odata or Redfish property."
				}
			},
			"additionalProperties": false,
			"properties": {
				"@odata.context": {
					"$ref": "http://redfish.dmtf.org/schemas/v1/odata.4.0.0.json#/definitions/context"
				},
				"@odata.id": {
					"$ref": "http://redfish.dmtf.org/schemas/v1/odata.4.0.0.json#/definitions/id"
				},
				"@odata.type": {
					"$ref": "http://redfish.dmtf.org/schemas/v1/odata.4.0.0.json#/definitions/type"
				},
				"Oem": {
					"$ref": "http://redfish.dmtf.org/schemas/v1/Resource.json#/definitions/Oem",
					"description": "This is the manufacturer/provider specific extension moniker used to divide the Oem object into sections.",
					"longDescription": "The value of this string shall be of the format for the reserved word *Oem*."
				},
				"Id": {
					"$ref": "http://redfish.dmtf.org/schemas/v1/Resource.json#/definitions/Id"
				},
				"Description": {
					"$ref": "http://redfish.dmtf.org/schemas/v1/Resource.json#/definitions/Description"
				},
				"Name": {
					"$ref": "http://redfish.dmtf.org/schemas/v1/Resource.json#/definitions/Name"
				},
				"SecureBootEnable": {
					"type": "boolean",
					"readonly": false,
					"description": "Enable or disable UEFI Secure Boot (takes effect on next boot).",
					"longDescription": "Setting this property to true enables UEFI Secure Boot, and setting it to false disables it. This property can be enabled only in UEFI boot mode."
				},
				"SecureBootCurrentState": {
					"description": "Current enabled state of Secure Boot.",
					"enum": [
						"Enabled",
						"Disabled"
					],
					"enumDescriptions": {
						"Enabled": "Secure Boot is currently enabled.",
						"Disabled": "Secure Boot is currently disabled."
					},
					"longDescription": "The value of this property shall indicate the current state of UEFI Secure Boot.",
					"readonly": true,
					"type": "string"
				},
				"SecureBootMode": {
					"description": "Current Secure Boot Mode.",
					"enum": [
						"SetupMode",
						"UserMode",
						"AuditMode",
						"DeployedMode"
					],
					"enumDescriptions": {
						"SetupMode": "Secure Boot is currently in Setup Mode.",
						"UserMode": "Secure Boot is currently in User Mode.",
						"AuditMode": "Secure Boot is currently in Audit Mode.",
						"DeployedMode": "Secure Boot is currently in Deployed Mode."
					},
					"longDescription": "This property shall contain the current Secure Boot mode, as defined in the UEFI Specification.",
					"readonly": true,
					"type": "string"
				},
                "ResetKeys": {
					"description": "Reset the UEFI Secure Boot key databases.",
					"enum": [
						"No",
						"ResetAllKeysToDefault",
						"ClearAllKeys",
						"ClearPK"
					],
					"enumDescriptions": {
						"No": "Do not reset Secure Boot key databases.",
						"ResetAllKeysToDefault": "Reset the content of all UEFI Secure Boot key databases (PK, KEK, DB, DBX) to their default values.",
						"ClearAllKeys": "Clear the content of all UEFI Secure Boot key databases (PK, KEK, DB, DBX). This puts the system in Setup Mode.",
						"ClearPK": "Clear the content of the PK UEFI Secure Boot database. This puts the system in Setup Mode"
					},
					"longDescription": "This property shall be used to clear the UEFI Secure Boot databases or set them to their default values.",
					"readonly": false,
					"type": "string"
                }
			}
		}
	},
	"copyright": "Copyright 2014-2016 Hewlett Packard Enterprise LP. All rights reserved."
}
